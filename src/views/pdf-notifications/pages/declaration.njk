{% extends "../layouts/page-layout.html" %}
{% from "../macros/header.njk" import header %}
{% from "../macros/address.njk" import address %}
{% from "../macros/returns-title.njk" import returnsTitle %}
{% from "../macros/cover-text.njk" import coverText %}
{% from "../macros/cover-details.njk" import coverDetails %}
{% from "../macros/footer.njk" import footer %}
{% from "../macros/boxes.njk" import boxes %}
{% from "../macros/checkbox.njk" import checkbox %}
{% from "../macros/input.njk" import input %}

{% block header %}
  {{ header(notification.personalisation) }}
{% endblock %}

{% block content %}
<div class="columns">
  <div class="column">

    <div class="block">
      <div class="block-head bold">10. Total volume of water abstracted</div>
      <p class="label">
        Amount abstracted in the return period
        {{ boxes() }}
      </p>
    </div>

    <div class="block">
      <div class="block-head bold">11. Reporting without a meter</div>
      <p class="label">
        What are the abstraction volumes based on? For example, pump capacity multiplied by hours run.
        {{ input(1) }}
      </p>
    </div>

    <div class="block">
      <div class="block-head bold">12. Further information</div>
      <div class="item">
        <p class="label">
          If you need to explain an entry or give more information send this on a
          separate sheet. Write the return reference
          <span class="bold">{{notification.personalisation.format_id}}</span>
          and licence number
          <span class="bold">{{notification.personalisation.licence_ref}}</span>
          on each sheet.
        </p>
      </div>
      <div class="checkbox-and-text">
        <div>
          <div class="checkbox"></div>
        </div>
        <div>
          <p>Tick this box if you are sending<br>further information</p>
        </div>
      </div>
    </div>

    <div class="block">
      <div class="block-head bold">13. Declaration</div>
      <p>I confirm that the details in this return are correct to the best of my knowledge.</p>
      <br>
      <p class="label">Signed</p>
      {{ input() }}
    </div>

    {% include '../includes/declaration-contact-us.html' %}
  </div>

  <div class="column">
    {% include '../includes/declaration-privacy.html' %}
  </div>
</div>

{% endblock %}

{% block footer %}
  {{ footer(totalPages, totalPages) }}
{% endblock %}
